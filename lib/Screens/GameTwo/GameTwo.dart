import 'package:flutter/material.dart';

class GameTwo extends StatelessWidget {
  const GameTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
